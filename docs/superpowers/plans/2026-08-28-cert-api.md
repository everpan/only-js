# sample 证书管理 API 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 sample/ 中提供 JWS 证书的在线管理 API——生成、renew、备注、列表/详情/删除（admin 专用），密码学在 Rust 侧经新 bridge 全局 `cert.*` 完成，材料存 sqlite。

**Architecture:** `tools/oj-cert` lib 抽出纯内存函数（keygen / sign_jws / pem），core 根 crate 以路径依赖复用并新增 `src/bridge/cert.rs` 两个同步 op，`bootstrap.js` 挂 `globalThis.cert`；sample 新增 `cert` 模块（3 个路由目录 + `_shared/guard.ts`），材料与备注存 `certs` 表。

**Tech Stack:** Rust（rsa 0.9 / deno_core op2 / serde_json）、sample handler JS/TS、`oj test` 进程内 L1 测试。

**Spec:** `docs/superpowers/specs/2026-08-28-cert-api-design.md`

## Global Constraints

- `src/bridge/bootstrap.js` 必须保持 7-bit ASCII（每步改完用 `LC_ALL=C grep -n '[^[:print:]]'` 验证，期望无输出）。
- 构建仅 release：验证类命令用 `cargo build --release` / `cargo test`（测试不经 profile 别名，允许直接跑）；禁止把任何 profile 覆盖为 `panic = "abort"`。
- 门禁：`cargo fmt --check` 与 `cargo clippy --all-targets --workspace -D warnings` 全绿。
- 根 crate 新依赖仅允许 `oj-cert = { path = "tools/oj-cert" }`（workspace 内路径依赖）。
- sample handler 只用注入全局（`json/db/http/cert` 等）与相对导入，不引 npm 包。
- 统一信封 `{code,msg,data}`；`json.fail(code,...)` 的 code 会映射为 HTTP 状态。
- 测试命令（sample）：`cargo run -p oj -- test -c sample/config.yaml -d sample/src`（在仓库根执行）。seed.sql 在 `App::from_config` 时重放进 default sqlite 库，`oj test` 同样走该路径。
- 提交信息以 `unix@vip.qq.com ai` 结尾（`git commit -m "<msg>" -m "unix@vip.qq.com ai"`）。

---

### Task 1: oj-cert lib 抽纯内存函数

**Files:**
- Modify: `tools/oj-cert/src/lib.rs`
- Test: `tools/oj-cert/src/lib.rs`（文件内 `#[cfg(test)]`）

**Interfaces:**
- Produces（Task 2 依赖，签名精确如下）:
  - `pub fn keygen(bits: u32) -> Result<RsaPrivateKey, String>`（bits < `MIN_BITS` 报错）
  - `pub fn sign_jws(key: &RsaPrivateKey, nbf: u64, exp: u64) -> String`
  - `pub fn private_pem(key: &RsaPrivateKey) -> Result<String, String>`（PKCS#8 PEM 文本）
  - `pub fn public_pem(key: &RsaPrivateKey) -> Result<String, String>`（SPKI PEM 文本）
- 保持不变: `r#gen` / `renew` 的文件行为、`GenOpts` / `RenewOpts` / `MIN_BITS` / `now_secs` / `days_to_expiry`。

- [ ] **Step 1: 写失败测试**

在 `tools/oj-cert/src/lib.rs` 的 `#[cfg(test)] mod tests` 中追加（保留现有 `days_are_days_not_seconds`）：

```rust
#[test]
fn keygen_rejects_small_bits() {
    assert!(keygen(1024).is_err());
    assert!(keygen(0).is_err());
}

#[test]
fn sign_jws_is_three_parts_with_nbf_exp_payload() {
    let key = keygen(2048).unwrap();
    let j = sign_jws(&key, 1_000, 2_000);
    let parts: Vec<&str> = j.split('.').collect();
    assert_eq!(parts.len(), 3);
    use base64::Engine;
    let payload = base64::engine::general_purpose::URL_SAFE_NO_PAD
        .decode(parts[1])
        .unwrap();
    let s = String::from_utf8(payload).unwrap();
    assert!(s.contains("\"nbf\"") && s.contains("1000"), "{s}");
    assert!(s.contains("\"exp\"") && s.contains("2000"), "{s}");
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cargo test -p oj-cert`
Expected: 编译失败（`keygen` / `sign_jws` 不存在）。

- [ ] **Step 3: 实现纯函数并重构 r#gen / renew / write_private**

在 `MIN_BITS` 常量后、`now_secs` 前插入四个 pub 函数；随后把 `r#gen` / `renew` / `write_private` 改为复用它们。改动后的关键片段（注释风格沿用文件现状）：

```rust
/// 生成 RSA 密钥对（bits 下限 MIN_BITS）。
pub fn keygen(bits: u32) -> Result<RsaPrivateKey, String> {
    if bits < MIN_BITS {
        return Err(format!("bits must be >= {MIN_BITS}"));
    }
    RsaPrivateKey::new(&mut OsRng, bits as usize).map_err(|e| format!("keygen: {e}"))
}

/// RS256 JWS 三段串（b64url no-pad，与 server 加载端 split('.') 对齐）。
pub fn sign_jws(key: &RsaPrivateKey, nbf: u64, exp: u64) -> String {
    jws(&SigningKey::<Sha256>::new(key.clone()), nbf, exp)
}

/// 私钥 → PKCS#8 PEM 文本。
pub fn private_pem(key: &RsaPrivateKey) -> Result<String, String> {
    let pem = key
        .to_pkcs8_pem(LineEnding::LF)
        .map_err(|e| format!("encode pkcs8: {e}"))?;
    // `as_bytes()` 对 String / SecretDocument 均成立，免依赖 pkcs8 具体版本返回类型。
    Ok(String::from_utf8_lossy(pem.as_bytes()).into_owned())
}

/// 公钥 → SPKI PEM 文本（loader 兼容）。
pub fn public_pem(key: &RsaPrivateKey) -> Result<String, String> {
    key.to_public_key()
        .to_public_key_pem(LineEnding::LF)
        .map_err(|e| format!("encode spki: {e}"))
}
```

`write_private` 改为：

```rust
fn write_private(path: &Path, key: &RsaPrivateKey) -> Result<(), String> {
    let pem = private_pem(key)?;
    std::fs::write(path, &pem).map_err(|e| format!("write {}: {e}", path.display()))?;
    #[cfg(unix)]
    {
        use std::os::unix::fs::PermissionsExt;
        std::fs::set_permissions(path, std::fs::Permissions::from_mode(0o600))
            .map_err(|e| format!("chmod {}: {e}", path.display()))?;
    }
    Ok(())
}
```

`r#gen` 中删去内联 bits 校验与 keygen（改用 `keygen(opts.bits)?`）、删去内联 PEM 编码（改用 `public_pem(&key)?` 与 `sign_jws(&key, opts.nbf, opts.exp)`）；`renew` 不变。`jws` 保持私有（`sign_jws` 是其公开包装）。函数文档注释（`r#gen` 的 edition 2024 说明等）原样保留。

- [ ] **Step 4: 跑测试确认通过**

Run: `cargo test -p oj-cert`
Expected: 全部 PASS（新 2 例 + 既有 `days_are_days_not_seconds`）。

- [ ] **Step 5: 提交**

```bash
git add tools/oj-cert/src/lib.rs
git commit -m "refactor(oj-cert): 抽出纯内存 keygen/sign_jws/pem 函数（CLI 行为不变）" -m "unix@vip.qq.com ai"
```

---

### Task 2: core bridge 新增 `cert.*` 全局

**Files:**
- Modify: `Cargo.toml`（根 crate `[dependencies]`）
- Create: `src/bridge/cert.rs`
- Modify: `src/bridge/mod.rs`（`pub mod` 列表 + extension! `ops` 列表）
- Modify: `src/bridge/bootstrap.js`（挂 `globalThis.cert`）
- Modify: `sample/global.d.ts`（`CertApi` 类型）
- Test: `src/bridge/cert.rs`（文件内 `#[cfg(test)]`，经 `Bridge::run_with` 跑真实 runtime）

**Interfaces:**
- Consumes: Task 1 的 `oj_cert::{keygen, sign_jws, private_pem, public_pem}`。
- Produces（Task 4 依赖）: JS 全局
  - `cert.generate(bits: number, nbf: number, exp: number): Promise<{private_pem: string, public_pem: string, cert_jws: string}>`
  - `cert.renew(privatePem: string, nbf: number, exp: number): Promise<string>`（返回新 cert_jws）
  - （op 为同步，JS 侧 `await` 普通值合法，handler 统一按 Promise 用。）

- [ ] **Step 1: 加依赖**

根 `Cargo.toml` `[dependencies]` 追加一行：

```toml
oj-cert = { path = "tools/oj-cert" }
```

- [ ] **Step 2: 写失败测试 + todo!() 桩**

创建 `src/bridge/cert.rs`，模块头注释 + op 桩（`todo!()`）+ 测试：

```rust
//! cert 全局对象：JWS 证书生成与重签（tools/oj-cert 的桥接；纯内存，不落盘）。
//!
//! 格式契约（header `{"alg":"RS256","typ":"JWT"}` + payload `{nbf,exp}` + b64url
//! no-pad）的单一事实来源在 tools/oj-cert；CLI 与本模块同源。验签与状态判定在
//! server/src/certificate.rs。

use deno_error::JsErrorBox;
use rsa::pkcs8::DecodePrivateKey;
use serde_json::json;

fn to_unix(v: i64) -> Result<u64, JsErrorBox> {
    u64::try_from(v).map_err(|_| JsErrorBox::generic("timestamp must be >= 0"))
}

/// cert.generate(bits, nbf, exp)：生成 RSA 密钥对 + JWS，返回三件套（内存字符串）。
#[deno_core::op2]
#[serde]
pub fn op_cert_gen(bits: u32, nbf: i64, exp: i64) -> Result<serde_json::Value, JsErrorBox> {
    todo!()
}

/// cert.renew(private_pem, nbf, exp)：读 PKCS#8 私钥重签，返回新 cert.jws（公钥不变）。
#[deno_core::op2]
#[string]
pub fn op_cert_renew(
    #[string] private_pem: String,
    nbf: i64,
    exp: i64,
) -> Result<String, JsErrorBox> {
    todo!()
}

#[cfg(test)]
mod tests {
    use super::*;
    use crate::bridge::{Bridge, InMemoryAccessor, InMemoryKV, RequestInfo};
    use serde_json::Value;
    use std::sync::Arc;

    async fn run(js: &str) -> Value {
        let b = Bridge::new(Arc::new(InMemoryAccessor::new()), Arc::new(InMemoryKV::new()));
        let cap = b.run_with(js, RequestInfo::default()).await.unwrap();
        serde_json::from_slice(&cap.body).unwrap()
    }

    const GEN: &str = r#"(async () => { json.ok(await cert.generate(2048, 1000, 2000)); })()
        .catch((e) => json.fail(500, String(e)));"#;

    #[tokio::test(flavor = "current_thread")]
    async fn generate_returns_three_part_jws_and_pems() {
        let v = run(GEN).await;
        assert_eq!(v["code"], 0, "{v}");
        let m = &v["data"];
        assert_eq!(m["cert_jws"].as_str().unwrap().split('.').count(), 3, "{v}");
        assert!(
            m["public_pem"].as_str().unwrap().starts_with("-----BEGIN PUBLIC KEY"),
            "{v}"
        );
        assert!(m["private_pem"].as_str().unwrap().contains("PRIVATE KEY"), "{v}");
    }

    #[tokio::test(flavor = "current_thread")]
    async fn renew_reuses_key_and_changes_jws() {
        let v = run(
            r#"(async () => {
                const m = await cert.generate(2048, 1000, 2000);
                const jws2 = await cert.renew(m.private_pem, 3000, 4000);
                json.ok({ jws1: m.cert_jws, jws2, pub: m.public_pem });
            })().catch((e) => json.fail(500, String(e)));"#,
        )
        .await;
        assert_eq!(v["code"], 0, "{v}");
        let d = &v["data"];
        assert_ne!(d["jws1"], d["jws2"], "{v}");
        assert_eq!(d["jws2"].as_str().unwrap().split('.').count(), 3, "{v}");
        assert!(d["pub"].as_str().unwrap().starts_with("-----BEGIN PUBLIC KEY"), "{v}");
    }

    #[tokio::test(flavor = "current_thread")]
    async fn rejects_bad_bits_and_bad_window() {
        let v = run(
            r#"(async () => { await cert.generate(1024, 1000, 2000); json.ok({}); })()
                .catch((e) => json.ok({ err: String(e) }));"#,
        )
        .await;
        assert!(
            v["data"]["err"].as_str().unwrap().contains("bits must be >="),
            "{v}"
        );
        let v = run(
            r#"(async () => { await cert.generate(2048, 2000, 2000); json.ok({}); })()
                .catch((e) => json.ok({ err: String(e) }));"#,
        )
        .await;
        assert!(
            v["data"]["err"]
                .as_str()
                .unwrap()
                .contains("exp must be greater than nbf"),
            "{v}"
        );
    }
}
```

`src/bridge/mod.rs`：在模块声明区加 `pub mod cert;`（与其他轴并列）；extension! 的 `ops = [...]` 列表尾部（`ws::op_ws_close` 前）加：

```rust
        cert::op_cert_gen,
        cert::op_cert_renew,
```

- [ ] **Step 3: 跑测试确认失败**

Run: `cargo test cert::`
Expected: 编译通过（桩体 `todo!()`），3 个测试 PANIC（not yet implemented）。

- [ ] **Step 4: 实现两个 op**

替换两个 `todo!()`：

```rust
#[deno_core::op2]
#[serde]
pub fn op_cert_gen(bits: u32, nbf: i64, exp: i64) -> Result<serde_json::Value, JsErrorBox> {
    let nbf = to_unix(nbf)?;
    let exp = to_unix(exp)?;
    if exp <= nbf {
        return Err(JsErrorBox::generic("exp must be greater than nbf"));
    }
    let key = oj_cert::keygen(bits).map_err(JsErrorBox::generic)?;
    Ok(json!({
        "private_pem": oj_cert::private_pem(&key).map_err(JsErrorBox::generic)?,
        "public_pem": oj_cert::public_pem(&key).map_err(JsErrorBox::generic)?,
        "cert_jws": oj_cert::sign_jws(&key, nbf, exp),
    }))
}

#[deno_core::op2]
#[string]
pub fn op_cert_renew(
    #[string] private_pem: String,
    nbf: i64,
    exp: i64,
) -> Result<String, JsErrorBox> {
    let nbf = to_unix(nbf)?;
    let exp = to_unix(exp)?;
    if exp <= nbf {
        return Err(JsErrorBox::generic("exp must be greater than nbf"));
    }
    let key = rsa::RsaPrivateKey::from_pkcs8_pem(&private_pem).map_err(|e| {
        JsErrorBox::generic(format!("parse private key (PKCS#8 PEM): {e}"))
    })?;
    Ok(oj_cert::sign_jws(&key, nbf, exp))
}
```

- [ ] **Step 5: 跑测试确认通过**

Run: `cargo test cert::`
Expected: 3 个测试 PASS（2048 位 keygen 每例 <1s）。

- [ ] **Step 6: bootstrap.js 挂全局 + global.d.ts 类型**

`src/bridge/bootstrap.js`：import 区加 `op_cert_gen,` `op_cert_renew,`（按字母序插入现有列表）；文件尾部（`__ojRequire` 段之后）追加（**仅 ASCII**）:

```js
// ----- cert: JWS certificate issue/renew (RSA keygen + RS256 signing live in Rust) -----
// generate -> {private_pem, public_pem, cert_jws}; renew -> new cert_jws (same public key).
globalThis.cert = {
  generate: (bits, nbf, exp) => op_cert_gen(bits | 0, nbf, exp),
  renew: (privatePem, nbf, exp) => op_cert_renew(String(privatePem), nbf, exp),
};
```

ASCII 验证：`LC_ALL=C grep -n '[^[:print:]]' src/bridge/bootstrap.js`，期望无输出。

`sample/global.d.ts`：在 `declare global { ... }` 块内 `const db: DBInstance;` 之后加 `const cert: CertApi;`；块外（`interface BusApi` 附近）加：

```ts
// cert.* ：JWS 证书生成/重签（RSA + RS256 在 Rust 侧；纯内存，不落盘）。
interface CertApi {
  // 生成密钥对并签发 JWS。bits >= 2048；nbf/exp 为 Unix 秒，exp 必须 > nbf。
  generate(
    bits: number,
    nbf: number,
    exp: number,
  ): Promise<{ private_pem: string; public_pem: string; cert_jws: string }>;
  // 用现有 PKCS#8 私钥重签续期（公钥不变），返回新 cert.jws 串。
  renew(privatePem: string, nbf: number, exp: number): Promise<string>;
}
```

- [ ] **Step 7: 全量门禁**

Run: `cargo fmt && cargo clippy --all-targets --workspace -D warnings && cargo test --workspace`
Expected: 全绿（若 `cargo test --workspace` 中 sample e2e 需要插件，失败与本任务无关时以 `cargo test` 为准记录说明）。

- [ ] **Step 8: 提交**

```bash
git add Cargo.toml Cargo.lock src/bridge/cert.rs src/bridge/mod.rs src/bridge/bootstrap.js sample/global.d.ts
git commit -m "feat(bridge): cert 全局对象——oj-cert gen/renew 桥接（纯内存，不落盘）" -m "unix@vip.qq.com ai"
```

---

### Task 3: sample 种子数据（certs 表 + trinity 测试账号）

**Files:**
- Modify: `sample/seed.sql`

**Interfaces:**
- Produces（Task 4/5 依赖）: `certs` 表（列见 Step 1）；`users` 表 trinity 行（密码 `demo1234`，roles `["user"]`）。

- [ ] **Step 1: 追加 SQL**

`sample/seed.sql` 尾部追加：

```sql
CREATE TABLE IF NOT EXISTS certs (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  name TEXT NOT NULL,
  note TEXT NOT NULL DEFAULT '',
  public_pem TEXT NOT NULL,
  private_pem TEXT NOT NULL,
  cert_jws TEXT NOT NULL,
  nbf INTEGER NOT NULL,
  exp INTEGER NOT NULL,
  created_at INTEGER NOT NULL,
  updated_at INTEGER NOT NULL
);
-- trinity：user 角色测试账号（password_hash 与 demo 行相同，即密码 demo1234；
-- 仅供 403 用例，样例数据勿用于生产）。
INSERT OR IGNORE INTO users (id, username, password_hash, roles)
  VALUES (2, 'trinity',
    '$2b$10$aKN7gpFP.dhK7Il8sc19neUPaziSONYdsfks1xm0H2COzkp2vlqV2', '["user"]');
```

- [ ] **Step 2: 验证 seed 重放**

Run: `cargo run -p oj -- test -c sample/config.yaml -d sample/src --format human`
Expected: 既有测试（auth/news/tenant/user）全部 PASS——证明 seed 追加后 `App` 装配未破坏（CREATE/INSERT 重放成功；重复运行因 `IF NOT EXISTS`/`OR IGNORE` 幂等）。

- [ ] **Step 3: 提交**

```bash
git add sample/seed.sql
git commit -m "feat(sample): certs 表与 trinity 测试账号种子" -m "unix@vip.qq.com ai"
```

---

### Task 4: sample `cert` 模块（guard + 3 个路由）

**Files:**
- Create: `sample/src/cert/manifest.yaml`
- Create: `sample/src/cert/_shared/guard.ts`
- Create: `sample/src/cert/api.ts`
- Create: `sample/src/cert/item/api.ts`
- Create: `sample/src/cert/renew/api.ts`

**Interfaces:**
- Consumes: Task 2 的 `cert.generate` / `cert.renew`；Task 3 的 `certs` 表。
- Produces: 路由 `/v1/api/cert/`（GET 列表、POST 创建）、`/v1/api/cert/item/`（GET/PATCH/DELETE）、`/v1/api/cert/renew/`（POST）；全部要求 `http.user.roles` 含 `admin`（否则 `json.fail(403, "admin only")`）。

- [ ] **Step 1: manifest**

`sample/src/cert/manifest.yaml`：

```yaml
name: "cert"
desc: "证书管理：JWS 证书生成、renew、备注（admin）"
version: "0.1.0"
```

- [ ] **Step 2: guard（共享门禁 + 状态推导）**

`sample/src/cert/_shared/guard.ts`（无 api.ts → 非路由目录，模式同 `user/_shared/validate.ts`）：

```ts
// cert 模块共享：admin 门禁、参数校验、证书状态推导（无 api.ts → 非路由目录）。

// admin 门禁：http.user 由 auth 中间件验签注入；缺失或非 admin 视为拒绝。
export function isAdmin(): boolean {
  return !!http.user && (http.user.roles ?? []).includes("admin");
}

// 有效天数：整数 1..=3650，非法返回 null。
export function parseDays(raw: unknown): number | null {
  const d = Number(raw ?? 365);
  return Number.isInteger(d) && d >= 1 && d <= 3650 ? d : null;
}

// 证书状态：与 server/src/certificate.rs 判定一致（grace 缺省 30 天）。
export function statusOf(nbf: number, exp: number, now: number): string {
  if (now < exp) return "valid";
  if (now < exp + 30 * 86400) return "grace";
  return "expired";
}
```

- [ ] **Step 3: 列表 + 创建**

`sample/src/cert/api.ts`：

```ts
import { isAdmin, parseDays, statusOf } from "./_shared/guard";

// GET /v1/api/cert/ ：列表（不含 private_pem），附 status。
async function get(): Promise<void> {
  if (!isAdmin()) { json.fail(403, "admin only"); return; }
  try {
    const rows = await db.query(
      "select id, name, note, public_pem, cert_jws, nbf, exp, created_at, updated_at from certs order by id",
      [],
    );
    const now = Math.floor(Date.now() / 1000);
    json.ok(rows.map((r) => ({ ...r, status: statusOf(r.nbf as number, r.exp as number, now) })));
  } catch (e) {
    json.fail(500, String(e));
  }
}

// POST /v1/api/cert/ ：{name, note?, days?=365, bits?=2048} → 生成并入库。
async function post(): Promise<void> {
  if (!isAdmin()) { json.fail(403, "admin only"); return; }
  const b = http.body as { name?: string; note?: string; days?: number; bits?: number };
  const name = (b.name ?? "").trim();
  if (!name || name.length > 128) { json.fail(400, "name required (<=128 chars)"); return; }
  const days = parseDays(b.days);
  if (!days) { json.fail(400, "days must be integer 1..=3650"); return; }
  const bits = Number(b.bits ?? 2048);
  if (bits !== 2048 && bits !== 3072 && bits !== 4096) {
    json.fail(400, "bits must be 2048|3072|4096"); return;
  }
  try {
    const now = Math.floor(Date.now() / 1000);
    const exp = now + days * 86400;
    const m = await cert.generate(bits, now, exp);
    await db.exec(
      "insert into certs (name, note, public_pem, private_pem, cert_jws, nbf, exp, created_at, updated_at) values (?, ?, ?, ?, ?, ?, ?, ?, ?)",
      [name, (b.note ?? "").slice(0, 2000), m.public_pem, m.private_pem, m.cert_jws, now, exp, now, now],
    );
    const rows = await db.query("select last_insert_rowid() as id", []);
    json.ok({ id: rows[0]?.id, name });
  } catch (e) {
    json.fail(500, String(e));
  }
}

export default { get, post };
```

- [ ] **Step 4: 详情 / 备注 / 删除**

`sample/src/cert/item/api.ts`：

```ts
import { isAdmin, statusOf } from "../_shared/guard";

// GET ?id= ：详情（含 private_pem，管理员取用）。
async function get(): Promise<void> {
  if (!isAdmin()) { json.fail(403, "admin only"); return; }
  const id = Number(http.param("id", 0));
  if (!Number.isInteger(id) || id <= 0) { json.fail(400, "id required"); return; }
  try {
    const rows = await db.query(
      "select id, name, note, public_pem, private_pem, cert_jws, nbf, exp, created_at, updated_at from certs where id = ?",
      [id],
    );
    if (!rows.length) { json.fail(404, "cert not found"); return; }
    const r = rows[0];
    json.ok({ ...r, status: statusOf(r.nbf as number, r.exp as number, Math.floor(Date.now() / 1000)) });
  } catch (e) {
    json.fail(500, String(e));
  }
}

// PATCH {id, note} ：改备注。
async function patch(): Promise<void> {
  if (!isAdmin()) { json.fail(403, "admin only"); return; }
  const b = http.body as { id?: number; note?: string };
  const id = Number(b.id ?? 0);
  if (!Number.isInteger(id) || id <= 0) { json.fail(400, "id required"); return; }
  try {
    const n = await db.exec("update certs set note = ?, updated_at = ? where id = ?", [
      String(b.note ?? "").slice(0, 2000),
      Math.floor(Date.now() / 1000),
      id,
    ]);
    if (!n) { json.fail(404, "cert not found"); return; }
    json.ok({ updated: true });
  } catch (e) {
    json.fail(500, String(e));
  }
}

// DELETE ?id= ：删除。
async function del(): Promise<void> {
  if (!isAdmin()) { json.fail(403, "admin only"); return; }
  const id = Number(http.param("id", 0));
  if (!Number.isInteger(id) || id <= 0) { json.fail(400, "id required"); return; }
  try {
    const n = await db.exec("delete from certs where id = ?", [id]);
    if (!n) { json.fail(404, "cert not found"); return; }
    json.ok({ deleted: true });
  } catch (e) {
    json.fail(500, String(e));
  }
}

export default { get, patch, del };
```

- [ ] **Step 5: renew**

`sample/src/cert/renew/api.ts`：

```ts
import { isAdmin, parseDays } from "../_shared/guard";

// POST {id, days?=365} ：读库中私钥重签，公钥与 private_pem 不变。
async function post(): Promise<void> {
  if (!isAdmin()) { json.fail(403, "admin only"); return; }
  const b = http.body as { id?: number; days?: number };
  const id = Number(b.id ?? 0);
  if (!Number.isInteger(id) || id <= 0) { json.fail(400, "id required"); return; }
  const days = parseDays(b.days);
  if (!days) { json.fail(400, "days must be integer 1..=3650"); return; }
  try {
    const rows = await db.query("select private_pem from certs where id = ?", [id]);
    if (!rows.length) { json.fail(404, "cert not found"); return; }
    const now = Math.floor(Date.now() / 1000);
    const exp = now + days * 86400;
    const certJws = await cert.renew(String(rows[0].private_pem), now, exp);
    const n = await db.exec(
      "update certs set cert_jws = ?, nbf = ?, exp = ?, updated_at = ? where id = ?",
      [certJws, now, exp, now, id],
    );
    if (!n) { json.fail(404, "cert not found"); return; }
    json.ok({ renewed: true, nbf: now, exp });
  } catch (e) {
    json.fail(500, String(e));
  }
}

export default { post };
```

- [ ] **Step 6: 冒烟验证（dev server + curl）**

```bash
cargo run -p oj -- server -c sample/config.yaml -d sample/src &
sleep 2
TOK=$(curl -s -X POST http://localhost:9778/v1/api/auth/login -H 'Content-Type: application/json' -d '{"username":"demo","password":"demo1234"}' | sed -n 's/.*"access_token":"\([^"]*\)".*/\1/p')
# 列表（admin）
curl -s http://localhost:9778/v1/api/cert/ -H "Authorization: Bearer $TOK" -H 'X-TENANT-ID: default'
# 创建（含 2048 位 keygen，首次约 0.1-1s）
curl -s -X POST http://localhost:9778/v1/api/cert/ -H "Authorization: Bearer $TOK" -H 'X-TENANT-ID: default' -H 'Content-Type: application/json' -d '{"name":"smoke","note":"n1","days":30}'
# 无 token → 403/401
curl -s http://localhost:9778/v1/api/cert/ -H 'X-TENANT-ID: default'
kill %1
```

Expected: 创建返回 `{code:0,data:{id:1,...}}`；无 token 请求返回 401（auth 中间件先行）。

- [ ] **Step 7: 提交**

```bash
git add sample/src/cert/
git commit -m "feat(sample): cert 模块——生成/renew/备注/列表/详情/删除（admin）" -m "unix@vip.qq.com ai"
```

---

### Task 5: L1 集成测试 + 全量门禁

**Files:**
- Create: `sample/tests/cert.test.ts`

**Interfaces:**
- Consumes: Task 4 全部路由 + Task 3 的 trinity 账号。
- Produces: `oj test` 套件新增 cert 用例（回归护栏）。

- [ ] **Step 1: 写测试**

`sample/tests/cert.test.ts`（风格对齐 `sample/tests/auth.test.ts`；client 路径相对 base，真实 axum 派发支持 query 串）：

```ts
// cert 模块 L1 集成测试：admin 生成/备注/renew/删除全链路；user 角色 403。
// 运行：oj test -c sample/config.yaml -d sample/src

const ADMIN = { Authorization: "", "X-TENANT-ID": "default" };
const USER = { Authorization: "", "X-TENANT-ID": "default" };

describe("cert", () => {
  beforeEach(async () => {
    ADMIN.Authorization = "Bearer " + (await client.login("demo", "demo1234"));
    USER.Authorization = "Bearer " + (await client.login("trinity", "demo1234"));
  });

  it("user role gets 403 on create", async () => {
    const r = await client.post("/cert/", {
      headers: USER,
      body: JSON.stringify({ name: "nope" }),
    });
    expect(r.status).toBe(403);
  });

  it("admin create → list valid → note → renew → delete", async () => {
    // 创建
    const created = await client.post("/cert/", {
      headers: ADMIN,
      body: JSON.stringify({ name: "it-cert", note: "first", days: 30 }),
    });
    expect(created.status).toBe(200);
    const createdBody = JSON.parse(created.body);
    expect(createdBody.code).toBe(0);
    const id = createdBody.data.id;
    expect(id).toBeTruthy();

    // 列表：可见且 status=valid、不含 private_pem
    const list = JSON.parse((await client.get("/cert/", { headers: ADMIN })).body);
    const row = list.data.find((x: any) => x.id === id);
    expect(row.status).toBe("valid");
    expect(row.note).toBe("first");
    expect(row.private_pem).toBeFalsy();

    // 改备注
    const patched = await client.patch("/cert/item/", {
      headers: ADMIN,
      body: JSON.stringify({ id, note: "updated note" }),
    });
    expect(patched.status).toBe(200);

    // 详情（含私钥）→ renew → exp 增长、公钥不变、备注保留
    const before = JSON.parse(
      (await client.get("/cert/item/?id=" + id, { headers: ADMIN })).body,
    ).data;
    expect(before.private_pem).toBeTruthy();
    const renewed = await client.post("/cert/renew/", {
      headers: ADMIN,
      body: JSON.stringify({ id, days: 365 }),
    });
    expect(renewed.status).toBe(200);
    const after = JSON.parse(
      (await client.get("/cert/item/?id=" + id, { headers: ADMIN })).body,
    ).data;
    expect(after.exp).toBeGreaterThan(before.exp);
    expect(after.public_pem).toBe(before.public_pem);
    expect(after.cert_jws).not.toBe(before.cert_jws);
    expect(after.note).toBe("updated note");

    // 删除 → 404
    expect((await client.del("/cert/item/?id=" + id, { headers: ADMIN })).status).toBe(200);
    const gone = await client.get("/cert/item/?id=" + id, { headers: ADMIN });
    expect(gone.status).toBe(404);

    // 参数校验：days 越界 → 400
    const bad = await client.post("/cert/", {
      headers: ADMIN,
      body: JSON.stringify({ name: "bad", days: 0 }),
    });
    expect(bad.status).toBe(400);
  });
});
```

- [ ] **Step 2: 跑测试确认通过**

Run: `cargo run -p oj -- test -c sample/config.yaml -d sample/src --format human`
Expected: cert 2 例 PASS，既有套件不回归。

- [ ] **Step 3: 全量门禁**

Run: `cargo fmt --check && cargo clippy --all-targets --workspace -D warnings && cargo test --workspace`
Expected: 全绿。

- [ ] **Step 4: 提交**

```bash
git add sample/tests/cert.test.ts
git commit -m "test(sample): cert 模块 L1 集成测试（403/全链路/参数校验）" -m "unix@vip.qq.com ai"
```

---

## 自审记录

- **Spec 覆盖**：spec §改动清单 1→Task 1、2→Task 2、3（表/账号）→Task 3、3（handler）→Task 4、4 鉴权校验→guard.ts+各 handler、错误处理→各 handler `json.fail`、测试→Task 5。非目标（应用到自身、ACME、私钥加密）无任务，正确。
- **类型一致性**：`cert.generate(bits,nbf,exp)` / `cert.renew(pem,nbf,exp)` 在 Task 2（bootstrap+global.d.ts）、Task 4（handler）、Task 5（测试）三处一致；guard 导出 `isAdmin/parseDays/statusOf` 与 import 一致。
- **已知风险**：`to_pkcs8_pem` 返回类型随 pkcs8 版本不同（String vs SecretDocument）——`private_pem` 用 `pem.as_bytes()` 兼容两种；若编译报错按实际类型微调（行为不变）。
