[package]
name = "plugin"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
stabby = "72"
tokio = { version = "1", features = ["rt-multi-thread", "time", "sync", "macros"] }
