target/
.idea/
.vscode/
.claude/
.superpowers/
.codebuddy/
# 编译产物统一归置到 bin/（主程序 oj + 插件 cdylib 构件 bin/plugins/<triple>），
# 由 cargo xtask bin / plugin / build 产出，整目录忽略。
bin/
# 服务端按天滚动日志目录（默认 ./logs，可在 server.logs_dir 配置）。
logs/
CLAUDE.md
CODEBUDDY.md
CODEBUDDY_cn.md

