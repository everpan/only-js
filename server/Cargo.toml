[package]
name = "mdm-server"
version = "0.1.0"
edition = "2024"

[dependencies]
mdm-base-rust = { path = ".." }
axum = { version = "0.8", features = ["ws"] }
serde_json = "1"
tokio = { version = "1", features = ["macros", "rt", "rt-multi-thread", "net", "sync"] }
futures-util = "0.3.34"

[dev-dependencies]
serde_json = "1"
